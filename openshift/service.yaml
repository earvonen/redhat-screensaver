apiVersion: v1
kind: Service
metadata:
  name: redhat-screensaver
  labels:
    app: redhat-screensaver
spec:
  selector:
    app: redhat-screensaver
  ports:
    - name: http
      port: 80
      targetPort: http
